# ARCHITECTURE

## 1. Purpose

This document defines the system architecture of this repository.

The primary goal is:

- `apps/site` must remain permanently static-exportable
- GitHub Pages is the default deployment target in Phase 1
- Future dynamic expansion must not break static export
- Architectural boundaries must be explicit and enforced

---

## 2. Monorepo Structure

